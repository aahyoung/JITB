package com.user.main.purchase;

import com.user.frame.PurchasePanelFrame;
import com.user.main.ClientMain;

public class CouponMessage extends PurchasePanelFrame{
	public CouponMessage(ClientMain main) {
		super(main);
	}
}
